import 'package:flutter/material.dart';

class SettingColor extends StatelessWidget {
  const SettingColor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
